# Direct runtime dependencies for sgrep, pinned exactly so an image rebuilt
# next month behaves like one built today.
#
# No torch: queries run the model's prebuilt ONNX graph on onnxruntime, which
# imports in a fraction of the time and takes ~2GB off the image. See
# src/embedder.py.
onnxruntime==1.27.0
tokenizers==0.23.1
numpy==2.5.1

# Web demo layer (src/web/app.py).
fastapi==0.139.2
uvicorn[standard]==0.51.0
