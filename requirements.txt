# Direct runtime dependencies for sgrep.
# sentence-transformers pulls in torch + transformers transitively.
# In the demo image, CPU-only torch is installed separately (see deploy/Dockerfile)
# to avoid the multi-GB CUDA wheel.
# Version floors (not exact pins) because the runtime env wasn't available
# to capture verified versions; tighten these once you have a known-good env.
sentence-transformers>=3.0
numpy>=1.24

# Web demo layer (src/web/app.py).
fastapi>=0.110
uvicorn[standard]>=0.29
